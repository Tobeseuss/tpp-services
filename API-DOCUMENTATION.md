# مستندات REST API — سرویس‌های TPP (نسخه 1.20.0)

تمام امکانات افزونه از طریق REST API در دسترس است. این سند از همان منبعی تولید می‌شود که «مرکز API» داخل برنامه نمایش می‌دهد — همیشه همگام.

- آدرس پایه (پیوندهای یکتا روشن): `https://test.example.com/wp-json/tpp/v1/`
- آدرس جایگزین (پیوندهای یکتا خاموش): `https://test.example.com/?rest_route=%2Ftpp%2Fv1%2F`
- پشتیبان admin-ajax: `https://test.example.com/wp-admin/admin-ajax.php?action=tpp_api`

## احراز هویت

### توکن اختصاصی (پیشنهادی — همه‌جا کار می‌کند)

هدر X-TPP-Token: <token> — از login یا api/tokens بگیرید. حتی بدون کوکی/nonce معتبر است.

```bash
curl -H "X-TPP-Token: tpp_…" https://test.example.com/wp-json/tpp/v1/services/12
```

### کوکی وردپرس + nonce (داخل افزونه‌های PHP خود سایت)

برای درخواست‌های سمت سرور از خود وردپرس، کوکی‌های کاربر + هدر X-WP-Nonce: <nonce> را بفرستید (wp_create_nonce('wp_rest')).

```bash
curl -H "X-WP-Nonce: abc123" --cookie "wordpress_logged_in_…" https://test.example.com/wp-json/tpp/v1/search?query=تست
```

### پشتیبان admin-ajax (وقتی REST توسط افزونه امنیتی مسدود است)

admin-ajax.php?action=tpp_api&route=<مسیر>&method=<GET|POST|PUT|DELETE> — بدنه JSON در پارامتر body یا args؛ توکن در هدر یا پارامتر token.

```bash
curl -H "X-TPP-Token: tpp_…" "https://test.example.com/wp-admin/admin-ajax.php?action=tpp_api&route=services/12&method=GET"
```

## نکته‌های کلی

- همه پاسخ‌ها JSON هستند؛ خطاها به‌صورت {code, message, data:{status}}.
- دسترسی هر مسیر با نقش/قابلیت کاربر بررسی می‌شود و فیلدهای پنهان نقش او از خروجی حذف می‌شوند.
- عملیات‌های نوشتاری با op_id یکتا تکرارناپذیرند (idempotent) — برای اتصال سیستم‌های بی‌ثبات امن است.
- PHP API داخلی هم داریم: تابع tpp() — مستندات کامل در DEVELOPERS.md.
- ۱.۱۸.۰ — همه زمان‌های ثبت‌شده (created_at/updated_at/…) به وقت تهران (Asia/Tehran) هستند و نمایش اپ شمسی است؛ فیلترهای تاریخ سرور میلادی YYYY-MM-DD می‌گیرند.

## احراز هویت و نشست

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `ping` | وضعیت افزونه | عمومی | بررسی زنده بودن API + نسخه + آدرس ورود. در حالت ورود، nonce تازه هم برمی‌گرداند. |
| `POST` | `login` | ورود و دریافت توکن | عمومی | ورود با نام کاربری/رمز وردپرس → توکن X-TPP-Token برای همه فراخوانی‌های بعدی. |
| `GET` | `bootstrap` | راه‌اندازی اپ | کاربر افزونه | کاربر جاری + قابلیت‌ها + اسکیمای فیلدهای قابل مشاهده + تنظیمات + آمار — همه در یک فراخوانی. |
| `POST` | `logout` | خروج | کاربر افزونه | ابطال توکن فعلی؛ با all=1 همه دستگاه‌ها؛ با wp=1 خروج از وردپرس. |

### GET `ping` — وضعیت افزونه

بررسی زنده بودن API + نسخه + آدرس ورود. در حالت ورود، nonce تازه هم برمی‌گرداند.

دسترسی لازم: **عمومی**

### POST `login` — ورود و دریافت توکن

ورود با نام کاربری/رمز وردپرس → توکن X-TPP-Token برای همه فراخوانی‌های بعدی.

دسترسی لازم: **عمومی**

نمونه بدنه:

```json
{
    "username": "admin",
    "password": "•••"
}
```

### GET `bootstrap` — راه‌اندازی اپ

کاربر جاری + قابلیت‌ها + اسکیمای فیلدهای قابل مشاهده + تنظیمات + آمار — همه در یک فراخوانی.

دسترسی لازم: **کاربر افزونه**

### POST `logout` — خروج

ابطال توکن فعلی؛ با all=1 همه دستگاه‌ها؛ با wp=1 خروج از وردپرس.

دسترسی لازم: **کاربر افزونه**

نمونه بدنه:

```json
{
    "all": 1
}
```

## سرویس‌ها و آدرس‌ها

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `search` | جستجوی سراسری | مشاهده سرویس‌ها | جستجو در همه فیلدهای قابل جستجو (سرویس+آدرس) با فیلترهای فیلد، صفحه‌بندی، مرتب‌سازی، بازه زمانی ویرایش و فیلتر وضعیت پیشرفت دایری. فقط فیلدهای مجاز کاربر برگردانده می‌شود و عبارت/فیلتر در گزارش فعالیت ثبت می‌شود. |
| `POST` | `services` | ثبت سرویس | ثبت سرویس | ایجاد سرویس + تطبیق/ایجاد آدرس با کلید نرمال‌شده؛ op_id برای تکرارناپذیری؛ progress = مراحل دایری و خرابی (۱.۱۲.۰). |
| `GET` | `services/{id}` | مشخصات سرویس | مشاهده سرویس‌ها | کلید فیلد + progress (خلاصه مراحل دایری/خرابی) + تاریخچه + سرویس‌های همان آدرس + بازدیدهای اخیر؛ بازدید در گزارش فعالیت ثبت می‌شود. |
| `PUT` | `services/{id}` | ویرایش سرویس | ویرایش سرویس | ویرایش با base_version برای تشخیص تعارض همزمانی؛ op_id اختیاری؛ progress = مراحل دایری و خرابی (۱.۱۲.۰) — تغییرات در تاریخچه/گزارش فعالیت ثبت می‌شود. |
| `DELETE` | `services/{id}` | حذف سرویس | حذف سرویس | حذف + تاریخچه آبشاری؛ op_id اختیاری. |
| `GET` | `addresses/{id}` | آدرس و سرویس‌هایش | مشاهده سرویس‌ها | اطلاعات آدرس + همه سرویس‌های آن + تایم‌لاین. |
| `GET` | `addresses/suggest` | پیشنهاد آدرس | مشاهده سرویس‌ها | ?query= برای autocomplete آدرس در فرم ثبت. |
| `GET` | `values` | مقادیر یکتای فیلد | مشاهده سرویس‌ها | مقادیر متمایز یک فیلد برای کشویی‌های فیلتر (با جستجوی q). |

### GET `search` — جستجوی سراسری

جستجو در همه فیلدهای قابل جستجو (سرویس+آدرس) با فیلترهای فیلد، صفحه‌بندی، مرتب‌سازی، بازه زمانی ویرایش و فیلتر وضعیت پیشرفت دایری. فقط فیلدهای مجاز کاربر برگردانده می‌شود و عبارت/فیلتر در گزارش فعالیت ثبت می‌شود.

دسترسی لازم: **مشاهده سرویس‌ها**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `query` | string | عبارت جستجو (LIKE همه فیلدهای قابل جستجو) |
| `filters` | json | شیء فیلتر فیلد‌ها {f_center_name: "مرکز الف"} |
| `page / per_page` | int | صفحه‌بندی (پیش‌فرض ۲۵) |
| `sort / order` | string | updated\|created\|phone — ASC\|DESC |
| `group` | int | گروه‌بندی بر اساس آدرس (۱/۰) |
| `upd_from / upd_to` | date | بازه زمانی ویرایش YYYY-MM-DD |
| `progress_status` | string | وضعیت دایری: none\|progress\|done\|fail\|fail_los\|fail_phone\|fail_internet\|fail_other |
| `progress_step` | string | کلید مرحله خاص مثل fat یا inet_omc |
| `progress_step_state` | string | done = انجام‌شده / todo = انجام‌نشده (پیش‌فرض done) |
| `category` | int | ۱.۱۹.۰ — شناسه دسته‌بندی پروژه (فیلتر دقیق) |
| `tags` | string | ۱.۱۹.۰ — شناسه تگ‌ها با کاما — سرویسی که هرکدام را دارد |

### POST `services` — ثبت سرویس

ایجاد سرویس + تطبیق/ایجاد آدرس با کلید نرمال‌شده؛ op_id برای تکرارناپذیری؛ progress = مراحل دایری و خرابی (۱.۱۲.۰).

دسترسی لازم: **ثبت سرویس**

نمونه بدنه:

```json
{
    "address": {
        "f_postal_code": "1111111111"
    },
    "service": {
        "f_phone": "9100000000"
    },
    "progress": {
        "steps": [
            "infra",
            "fat"
        ],
        "failure": ""
    },
    "op_id": "uniq-1"
}
```

### GET `services/{id}` — مشخصات سرویس

کلید فیلد + progress (خلاصه مراحل دایری/خرابی) + تاریخچه + سرویس‌های همان آدرس + بازدیدهای اخیر؛ بازدید در گزارش فعالیت ثبت می‌شود.

دسترسی لازم: **مشاهده سرویس‌ها**

### PUT `services/{id}` — ویرایش سرویس

ویرایش با base_version برای تشخیص تعارض همزمانی؛ op_id اختیاری؛ progress = مراحل دایری و خرابی (۱.۱۲.۰) — تغییرات در تاریخچه/گزارش فعالیت ثبت می‌شود.

دسترسی لازم: **ویرایش سرویس**

نمونه بدنه:

```json
{
    "service": {
        "f_owner_name": "نام جدید"
    },
    "progress": {
        "steps": [
            "infra",
            "fat",
            "drop"
        ],
        "failure": "los"
    },
    "base_version": 3
}
```

### DELETE `services/{id}` — حذف سرویس

حذف + تاریخچه آبشاری؛ op_id اختیاری.

دسترسی لازم: **حذف سرویس**

نمونه بدنه:

```json
{
    "op_id": "uniq-2"
}
```

### GET `addresses/{id}` — آدرس و سرویس‌هایش

اطلاعات آدرس + همه سرویس‌های آن + تایم‌لاین.

دسترسی لازم: **مشاهده سرویس‌ها**

### GET `addresses/suggest` — پیشنهاد آدرس

?query= برای autocomplete آدرس در فرم ثبت.

دسترسی لازم: **مشاهده سرویس‌ها**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `query` | string | حداقل ۲ نویسه |

### GET `values` — مقادیر یکتای فیلد

مقادیر متمایز یک فیلد برای کشویی‌های فیلتر (با جستجوی q).

دسترسی لازم: **مشاهده سرویس‌ها**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `field` | string | slug فیلد مثل f_center_name |
| `q` | string | فیلتر LIKE |
| `limit` | int | پیش‌فرض ۱۰۰ |

## پیشرفت دایری سرویس (۱.۱۲.۰)

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `POST/PUT` | `services + progress` | ثبت/ویرایش پیشرفت دایری | ثبت/ویرایش سرویس | در بدنه ثبت (POST services) و ویرایش (PUT services/{id}) کلید progress ارسال می‌شود؛ ۱۶ کلید مرحله: infra, fat, drop, fusion_fat, atb, patch, modem, registered, ready, sip_info, inet_config, inet_omc, inet_connected, sip_config, sip_omc, phone_connected. خرابی: los (اینترنت+تلفن قطع) / phone (قطع تلفن) / internet (قطع اینترنت) / other (سایر — شرح در فیلد توضیحات). ۱.۱۳.۰ — منطق آبشاری: مراحل وابسته‌اند؛ تیک مرحله N همه مراحل قبل از N را خودکار تیک می‌زند «مگر مراحل ردشده» که بعد از تیک‌خوردن، تیکشان توسط کاربر برداشته شده (progress.skipped یا برداشتن تیک با وجود مرحله بعدی تیک‌دار → progress_excluded). ۱.۱۴.۰ — چند خرابی همزمان: progress.failures آرایه کلیدها (مثلاً ["internet","other"])؛ failure تکی قدیمی هم پذیرفته می‌شود. خروجی GET services/{id} و search کلید progress را با steps/failures/failures_labels/failure/failure_label/done/total/pct/status/last_label/excluded (مراحل ردشده) برمی‌گرداند. تغییرات پیشرفت با برچسب فارسی مراحل در تاریخچه و گزارش فعالیت ثبت می‌شوند. |
| `POST` | `services/bulk` | تغییر گروهی/تکی پیشرفت و وضعیت | ویرایش سریع (⚡/🚀) — فقط مدیر کل به‌طور پیش‌فرض | اعمال یک‌باره پیشرفت دایری + خرابی + فیلدهای سرویس روی فهرستی از سرویس‌ها (ids). mode: up_to = تنظیم تا مرحله (آبشاری — همه مراحل قبل خودکار تیک می‌خورند)، add/remove = افزودن/حذف مراحل خاص، clear = پاک‌کردن، خالی = فقط خرابی/فیلد. skipped_policy: keep (مراحل ردشده توسط کاربر حفظ شوند — پیش‌فرض) / reset (بدون استثنا). ۱.۱۴.۰ — failures: آرایه خرابی‌ها (null = بدون تغییر؛ آرایه خالی = رفع همه)؛ failure تکی قدیمی هم پذیرفته می‌شود. service: {slug: value} فیلدهای سرویس (مثل f_internet_status). هر تغییر در تاریخچه و گزارش فعالیت با علامت «تغییر گروهی» ثبت می‌شود؛ dry_run = محاسبه بدون ذخیره. دسترسی: قابلیت «ویرایش سریع» — به‌طور پیش‌فرض فقط مدیر کل سایت؛ برای نقش‌ها از «نقش‌ها و دسترسی‌ها» قابل اعطاست. |

### POST/PUT `services + progress` — ثبت/ویرایش پیشرفت دایری

در بدنه ثبت (POST services) و ویرایش (PUT services/{id}) کلید progress ارسال می‌شود؛ ۱۶ کلید مرحله: infra, fat, drop, fusion_fat, atb, patch, modem, registered, ready, sip_info, inet_config, inet_omc, inet_connected, sip_config, sip_omc, phone_connected. خرابی: los (اینترنت+تلفن قطع) / phone (قطع تلفن) / internet (قطع اینترنت) / other (سایر — شرح در فیلد توضیحات). ۱.۱۳.۰ — منطق آبشاری: مراحل وابسته‌اند؛ تیک مرحله N همه مراحل قبل از N را خودکار تیک می‌زند «مگر مراحل ردشده» که بعد از تیک‌خوردن، تیکشان توسط کاربر برداشته شده (progress.skipped یا برداشتن تیک با وجود مرحله بعدی تیک‌دار → progress_excluded). ۱.۱۴.۰ — چند خرابی همزمان: progress.failures آرایه کلیدها (مثلاً ["internet","other"])؛ failure تکی قدیمی هم پذیرفته می‌شود. خروجی GET services/{id} و search کلید progress را با steps/failures/failures_labels/failure/failure_label/done/total/pct/status/last_label/excluded (مراحل ردشده) برمی‌گرداند. تغییرات پیشرفت با برچسب فارسی مراحل در تاریخچه و گزارش فعالیت ثبت می‌شوند.

دسترسی لازم: **ثبت/ویرایش سرویس**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `progress.steps` | array | کلیدهای مراحل انجام‌شده (به هر ترتیبی — سرور مرتب/اعتبارسنجی/آبشاری می‌کند) |
| `progress.skipped` | array | کلیدهای مراحل ردشده توسط کاربر — آبشار از آن‌ها می‌پرد (اختیاری) |
| `progress.failures` | array | ۱.۱۴.۰ — آرایه خرابی‌ها: ["los","internet","phone","other"] — آرایه خالی = رفع همه خرابی‌ها |
| `progress.failure` | string | legacy — los \| phone \| internet \| other \| خالی (رفع خرابی) |

نمونه بدنه:

```json
{
    "progress": {
        "steps": [
            "infra",
            "fat",
            "drop"
        ],
        "skipped": [],
        "failures": [
            "internet",
            "other"
        ]
    }
}
```

### POST `services/bulk` — تغییر گروهی/تکی پیشرفت و وضعیت

اعمال یک‌باره پیشرفت دایری + خرابی + فیلدهای سرویس روی فهرستی از سرویس‌ها (ids). mode: up_to = تنظیم تا مرحله (آبشاری — همه مراحل قبل خودکار تیک می‌خورند)، add/remove = افزودن/حذف مراحل خاص، clear = پاک‌کردن، خالی = فقط خرابی/فیلد. skipped_policy: keep (مراحل ردشده توسط کاربر حفظ شوند — پیش‌فرض) / reset (بدون استثنا). ۱.۱۴.۰ — failures: آرایه خرابی‌ها (null = بدون تغییر؛ آرایه خالی = رفع همه)؛ failure تکی قدیمی هم پذیرفته می‌شود. service: {slug: value} فیلدهای سرویس (مثل f_internet_status). هر تغییر در تاریخچه و گزارش فعالیت با علامت «تغییر گروهی» ثبت می‌شود؛ dry_run = محاسبه بدون ذخیره. دسترسی: قابلیت «ویرایش سریع» — به‌طور پیش‌فرض فقط مدیر کل سایت؛ برای نقش‌ها از «نقش‌ها و دسترسی‌ها» قابل اعطاست.

دسترسی لازم: **ویرایش سریع (⚡/🚀) — فقط مدیر کل به‌طور پیش‌فرض**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `ids` | array | شناسه سرویس‌ها (حداکثر ۵۰۰۰) |
| `progress.mode` | string | up_to \| add \| remove \| clear \| خالی |
| `progress.step` | string | کلید مرحله برای up_to یا all = همه ۱۶ مرحله |
| `progress.steps` | array | کلیدها برای add/remove |
| `progress.skipped_policy` | string | keep (پیش‌فرض) \| reset |
| `progress.failures` | array | ۱.۱۴.۰ — آرایه خرابی‌ها؛ null=بدون تغییر \| [] = رفع همه \| ["los","other"] |
| `progress.failure` | string | legacy — null=بدون تغییر \| خالی=رفع \| los\|phone\|internet\|other |
| `service` | object | فیلدهای سرویس مثل {"f_internet_status": "فعال"} |
| `dry_run` | bool | true = فقط پیش‌نمایش بدون ذخیره |

نمونه بدنه:

```json
{
    "ids": [
        12,
        45,
        78
    ],
    "progress": {
        "mode": "up_to",
        "step": "inet_connected",
        "skipped_policy": "keep",
        "failures": [
            "internet"
        ]
    },
    "service": {
        "f_internet_status": "فعال"
    }
}
```

## بررسی موارد تکراری (مدیر کل)

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `duplicates` | گروه‌های تکراری + تشخیص تناقض | مدیر کل | سرویس‌های با مقدار یکسان در فیلدهای شناسایی؛ هر گروه: فیلدهای مشترک، فیلدهای متناقض (دلایل تناقض)، فیلدهای مکمل، وضعیت clean/conflict و سرویس اصلی پیشنهادی + خلاصه کلی. |
| `POST` | `duplicates/merge` | ادغام گروه | مدیر کل | ادغام اعضا در سرویس اصلی؛ فیلدهای خالی پر می‌شود و تاریخچه‌ها منتقل؛ pick = {slug: id} مقدار برندهٔ هر فیلد متناقض را تعیین می‌کند. |
| `POST` | `duplicates/merge_safe` | ادغام همه گروه‌های بدون تناقض | مدیر کل | گروه‌هایی که هیچ فیلد متناقضی ندارند یک‌جا و بی‌خطر ادغام می‌شوند؛ keys اختیاری برای محدود کردن. |
| `POST` | `duplicates/dismiss` | علامت «باقی می‌ماند» | مدیر کل | گروه از فهرست بررسی خارج می‌شود. |
| `POST` | `duplicates/restore` | بازگردانی علامت‌خورده‌ها | مدیر کل | key خاص یا all. |

### GET `duplicates` — گروه‌های تکراری + تشخیص تناقض

سرویس‌های با مقدار یکسان در فیلدهای شناسایی؛ هر گروه: فیلدهای مشترک، فیلدهای متناقض (دلایل تناقض)، فیلدهای مکمل، وضعیت clean/conflict و سرویس اصلی پیشنهادی + خلاصه کلی.

دسترسی لازم: **مدیر کل**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `fields` | string | فیلدهای شناسایی با کاما (پیش‌فرض: تلفن/شماره مجازی/سریال مودم) |
| `include_dismissed` | int | ۱ = گروه‌های علامت‌خورده هم بیایند |

### POST `duplicates/merge` — ادغام گروه

ادغام اعضا در سرویس اصلی؛ فیلدهای خالی پر می‌شود و تاریخچه‌ها منتقل؛ pick = {slug: id} مقدار برندهٔ هر فیلد متناقض را تعیین می‌کند.

دسترسی لازم: **مدیر کل**

نمونه بدنه:

```json
{
    "primary_id": 12,
    "ids": [
        12,
        45,
        78
    ],
    "pick": {
        "f_phone": 45
    }
}
```

### POST `duplicates/merge_safe` — ادغام همه گروه‌های بدون تناقض

گروه‌هایی که هیچ فیلد متناقضی ندارند یک‌جا و بی‌خطر ادغام می‌شوند؛ keys اختیاری برای محدود کردن.

دسترسی لازم: **مدیر کل**

نمونه بدنه:

```json
{
    "keys": [
        "12_45"
    ]
}
```

### POST `duplicates/dismiss` — علامت «باقی می‌ماند»

گروه از فهرست بررسی خارج می‌شود.

دسترسی لازم: **مدیر کل**

نمونه بدنه:

```json
{
    "ids": [
        12,
        45
    ]
}
```

### POST `duplicates/restore` — بازگردانی علامت‌خورده‌ها

key خاص یا all.

دسترسی لازم: **مدیر کل**

نمونه بدنه:

```json
{
    "key": "all"
}
```

## تاریخچه تغییرات

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `history` | گزارش تاریخچه | مشاهده تاریخچه | رکوردهای تجمیعی روزانه با فیلتر کاربر/عملیات/آدرس/تعارض؛ بدون cap مربوطه فقط تاریخچه خود کاربر. |
| `POST` | `history/{id}/restore` | بازگردانی وضعیت | ویرایش سرویس | اعمال مقادیر ابتدای همان روز. |
| `POST` | `history/delete` | حذف رکوردهای تاریخچه | حذف تاریخچه | حذف تکی/گروهی با {ids:[…]} — فقط مدیر. |

### GET `history` — گزارش تاریخچه

رکوردهای تجمیعی روزانه با فیلتر کاربر/عملیات/آدرس/تعارض؛ بدون cap مربوطه فقط تاریخچه خود کاربر.

دسترسی لازم: **مشاهده تاریخچه**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `user_id / action / entity / address_id / conflict` | mixed | فیلترها |
| `page / per_page` | int | صفحه‌بندی |

### POST `history/{id}/restore` — بازگردانی وضعیت

اعمال مقادیر ابتدای همان روز.

دسترسی لازم: **ویرایش سرویس**

### POST `history/delete` — حذف رکوردهای تاریخچه

حذف تکی/گروهی با {ids:[…]} — فقط مدیر.

دسترسی لازم: **حذف تاریخچه**

نمونه بدنه:

```json
{
    "ids": [
        1,
        2,
        3
    ]
}
```

## گزارش فعالیت

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `activity/log` | فید یکپارچه فعالیت | گزارش فعالیت | همه فعالیت‌ها (ایجاد/ویرایش/حذف/ادغام + بازدید + جستجو + پیامک) در یک فید مرتب با فیلتر کاربر/نوع/عملیات/سرویس/متن/بازه تاریخ. |
| `GET` | `activity/stats` | آمار فعالیت | گزارش فعالیت | شمار امروز/۷روز/۳۰روز + کاربران برتر + سرویس‌های پربازدید + عبارات پرتکرار + پنجره تجمیع جستجو. |
| `GET` | `activity/views` | بازدیدها | گزارش فعالیت | تاریخچه بازدید سرویس‌ها (ادغام ۱۵ دقیقه‌ای پیوسته). |
| `GET` | `activity/searches` | جستجوها | گزارش فعالیت | تاریخچه جستجو؛ جستجوهای در حال تایپ در پنجره search_dedupe_seconds یک رکورد می‌شوند. |
| `GET` | `activity/export` | خروجی CSV | گزارش فعالیت | دانلود CSV همان فید یکپارچه با همان فیلترها. |

### GET `activity/log` — فید یکپارچه فعالیت

همه فعالیت‌ها (ایجاد/ویرایش/حذف/ادغام + بازدید + جستجو + پیامک) در یک فید مرتب با فیلتر کاربر/نوع/عملیات/سرویس/متن/بازه تاریخ.

دسترسی لازم: **گزارش فعالیت**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `type` | string | change\|view\|search\|sms\|all |
| `action` | string | create\|update\|delete\|merge\|restore\|view\|search\|sms |
| `user_id / service_id / q` | mixed | فیلتر کاربر/سرویس/متن |
| `from / to` | date | بازه YYYY-MM-DD |
| `page / per_page` | int | صفحه‌بندی |

### GET `activity/stats` — آمار فعالیت

شمار امروز/۷روز/۳۰روز + کاربران برتر + سرویس‌های پربازدید + عبارات پرتکرار + پنجره تجمیع جستجو.

دسترسی لازم: **گزارش فعالیت**

### GET `activity/views` — بازدیدها

تاریخچه بازدید سرویس‌ها (ادغام ۱۵ دقیقه‌ای پیوسته).

دسترسی لازم: **گزارش فعالیت**

### GET `activity/searches` — جستجوها

تاریخچه جستجو؛ جستجوهای در حال تایپ در پنجره search_dedupe_seconds یک رکورد می‌شوند.

دسترسی لازم: **گزارش فعالیت**

### GET `activity/export` — خروجی CSV

دانلود CSV همان فید یکپارچه با همان فیلترها.

دسترسی لازم: **گزارش فعالیت**

## گزارش کار (۱.۱۴.۰ / ۱.۱۹.۰)

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `workreport` | گزارش کار روز / بازه‌ای | کاربر افزونه | گزارش روزانه اقدامات یک کاربر برای ارائه به مدیران — قلم‌های مرتب با شماره ترتیب. بدون user_id گزارش خود کاربر جاری؛ با user_id (فقط دارندگان قابلیت گزارش فعالیت) گزارش کاربر دیگر. ۱.۱۹.۰: با from/to (هر دو YYYY-MM-DD) گزارش بازه‌ای n-روزه/هفتگی/ماهانه با گروه‌بندی روزانه برمی‌گردد؛ پاسخ تک‌روز شامل feed (فعالیت‌های همان روز بدون جستجو + آدرس کامل/بلوک/پلاک/واحد/شماره مجازی سرویس)، activity_days (روزهای دارای فعالیت ماه برای هایلایت تقویم)، retention (مدت نگهداشت تاریخچه فعالیت برای اخطار) و actions (فهرست اقدامات آماده) است. |
| `POST` | `workreport` | افزودن قلم (دستی/API) | کاربر افزونه | افزودن قلم به گزارش روز جاری کاربر. با service_id + prefix متن قلم طبق قالب ساده‌شده ۱.۱۹.۰ ساخته می‌شود: «{اقدام} (آدرس کامل، نام خیابان یا بلوک، شماره پلاک، شماره واحد)، دایری سرویس تا مرحله (X)، مراحل باقیمانده بعدی از مرحله (Y)، خرابی اعلام‌شده: Z» (بدون شمارش مراحل/درصد). بدون سرویس، content خام ثبت می‌شود. |
| `POST` | `workreport/from_activity` | افزودن از تاریخچه فعالیت | کاربر افزونه | تبدیل یک ردیف فعالیت روز (بازدید یا تغییر سرویس) به قلم گزارش با قالب استاندارد: ردیف view → «رفع مشکل …»، ردیف change با action=create → «تحویل سرویس …»، سایر تغییرها → «رفع مشکل …». |
| `POST` | `workreport/line` | پیش‌نمایش متن قلم | کاربر افزونه | ساخت متن قالب برای یک سرویس بدون ثبت — برای پیش‌نمایش قبل از افزودن. |
| `PUT` | `workreport/{id}` | ویرایش قلم | کاربر افزونه | ویرایش متن/سرویس یک قلم — فقط مالک قلم. |
| `DELETE` | `workreport/{id}` | حذف قلم | کاربر افزونه | حذف قلم از گزارش روز — فقط مالک قلم. |

### GET `workreport` — گزارش کار روز / بازه‌ای

گزارش روزانه اقدامات یک کاربر برای ارائه به مدیران — قلم‌های مرتب با شماره ترتیب. بدون user_id گزارش خود کاربر جاری؛ با user_id (فقط دارندگان قابلیت گزارش فعالیت) گزارش کاربر دیگر. ۱.۱۹.۰: با from/to (هر دو YYYY-MM-DD) گزارش بازه‌ای n-روزه/هفتگی/ماهانه با گروه‌بندی روزانه برمی‌گردد؛ پاسخ تک‌روز شامل feed (فعالیت‌های همان روز بدون جستجو + آدرس کامل/بلوک/پلاک/واحد/شماره مجازی سرویس)، activity_days (روزهای دارای فعالیت ماه برای هایلایت تقویم)، retention (مدت نگهداشت تاریخچه فعالیت برای اخطار) و actions (فهرست اقدامات آماده) است.

دسترسی لازم: **کاربر افزونه**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `date` | date | روز گزارش YYYY-MM-DD (پیش‌فرض: امروز) |
| `from / to` | date | ۱.۱۹.۰ — بازه گزارش بازه‌ای (هر دو با هم) |
| `user_id` | int | شناسه کاربر — فقط برای مدیران |

### POST `workreport` — افزودن قلم (دستی/API)

افزودن قلم به گزارش روز جاری کاربر. با service_id + prefix متن قلم طبق قالب ساده‌شده ۱.۱۹.۰ ساخته می‌شود: «{اقدام} (آدرس کامل، نام خیابان یا بلوک، شماره پلاک، شماره واحد)، دایری سرویس تا مرحله (X)، مراحل باقیمانده بعدی از مرحله (Y)، خرابی اعلام‌شده: Z» (بدون شمارش مراحل/درصد). بدون سرویس، content خام ثبت می‌شود.

دسترسی لازم: **کاربر افزونه**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `date` | date | روز گزارش (پیش‌فرض امروز) |
| `content` | string | متن قلم (وقتی service_id ارسال نشود) |
| `service_id` | int | شناسه سرویس مرتبط (اختیاری) |
| `prefix` | string | عنوان اقدام مثل «رفع مشکل» / «تحویل سرویس» / متن دلخواه |

نمونه بدنه:

```json
{
    "date": "2026-06-12",
    "service_id": 12,
    "prefix": "رفع مشکل"
}
```

### POST `workreport/from_activity` — افزودن از تاریخچه فعالیت

تبدیل یک ردیف فعالیت روز (بازدید یا تغییر سرویس) به قلم گزارش با قالب استاندارد: ردیف view → «رفع مشکل …»، ردیف change با action=create → «تحویل سرویس …»، سایر تغییرها → «رفع مشکل …».

دسترسی لازم: **کاربر افزونه**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `src` | string | view \| change |
| `row_id` | int | شناسه رکورد همان جدول (بازدید/تاریخچه) |
| `date` | date | روز گزارش (پیش‌فرض امروز) |

نمونه بدنه:

```json
{
    "src": "change",
    "row_id": 345,
    "date": "2026-06-12"
}
```

### POST `workreport/line` — پیش‌نمایش متن قلم

ساخت متن قالب برای یک سرویس بدون ثبت — برای پیش‌نمایش قبل از افزودن.

دسترسی لازم: **کاربر افزونه**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `service_id` | int | شناسه سرویس |
| `prefix` | string | عنوان اقدام |

نمونه بدنه:

```json
{
    "service_id": 12,
    "prefix": "تحویل سرویس"
}
```

### PUT `workreport/{id}` — ویرایش قلم

ویرایش متن/سرویس یک قلم — فقط مالک قلم.

دسترسی لازم: **کاربر افزونه**

نمونه بدنه:

```json
{
    "content": "متن جدید"
}
```

### DELETE `workreport/{id}` — حذف قلم

حذف قلم از گزارش روز — فقط مالک قلم.

دسترسی لازم: **کاربر افزونه**

## دسته‌بندی پروژه‌ها و تگ‌ها (۱.۱۹.۰)

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `categories` | فهرست دسته‌بندی‌ها و تگ‌ها | کاربر افزونه | همه دسته‌بندی‌ها و تگ‌های سیستمی + تعداد استفاده — برای فرم ثبت/ویرایش سرویس و فیلترهای جستجو. |
| `POST` | `categories` | افزودن | دسته‌بندی پروژه‌ها (پیش‌فرض فقط مدیر کل) | {kind: category\|tag, label} — دسته/تگ جدید. |
| `PUT` | `categories/{id}` | ویرایش | دسته‌بندی پروژه‌ها | {label, sort_order}. |
| `DELETE` | `categories/{id}` | حذف | دسته‌بندی پروژه‌ها | دسته در حال استفاده حذف نمی‌شود؛ تگ از سرویس‌ها جدا و حذف می‌شود. |
| `GET` | `activity/days` | روزهای دارای فعالیت | کاربر افزونه | ?from&to[&user_id] — روزهایی که کاربر فعالیت (تغییر/بازدید/جستجو/پیامک) ثبت کرده؛ برای هایلایت تقویم گزارش کار. |

### GET `categories` — فهرست دسته‌بندی‌ها و تگ‌ها

همه دسته‌بندی‌ها و تگ‌های سیستمی + تعداد استفاده — برای فرم ثبت/ویرایش سرویس و فیلترهای جستجو.

دسترسی لازم: **کاربر افزونه**

### POST `categories` — افزودن

{kind: category|tag, label} — دسته/تگ جدید.

دسترسی لازم: **دسته‌بندی پروژه‌ها (پیش‌فرض فقط مدیر کل)**

نمونه بدنه:

```json
{
    "kind": "category",
    "label": "پروژه سازمانی"
}
```

### PUT `categories/{id}` — ویرایش

{label, sort_order}.

دسترسی لازم: **دسته‌بندی پروژه‌ها**

### DELETE `categories/{id}` — حذف

دسته در حال استفاده حذف نمی‌شود؛ تگ از سرویس‌ها جدا و حذف می‌شود.

دسترسی لازم: **دسته‌بندی پروژه‌ها**

### GET `activity/days` — روزهای دارای فعالیت

?from&to[&user_id] — روزهایی که کاربر فعالیت (تغییر/بازدید/جستجو/پیامک) ثبت کرده؛ برای هایلایت تقویم گزارش کار.

دسترسی لازم: **کاربر افزونه**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `from / to` | date | بازه YYYY-MM-DD |

## بازبینی (۱.۲۰.۰)

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `review/queue` | صف سرویس‌های ارجاعی | قابلیت «بازبینی سرویس‌های ارجاعی» (پیش‌فرض فقط مدیر کل؛ از نقش‌ها قابل اعطا) | سرویس‌هایی که با دسته‌بندی پیش‌فرض «ثبت جهت بازبینی و ویرایش یا تأیید مدیریت» ثبت شده‌اند — همراه آدرس کامل/بلوک/پلاک/واحد، شماره مجازی، ثبت‌کننده، وضعیت دایری و فهرست دسته‌ها/تگ‌ها برای تعیین دسته. هر سرویسی که دسته مناسب ندارد می‌تواند با این دسته به بازبینی ارجاع شود. |
| `POST` | `review/assign` | تعیین دسته سرویس ارجاعی | بازبینی سرویس‌های ارجاعی | {service_id, category, tags} — بازبین دسته‌بندی/تگ درست را تعیین می‌کند؛ سرویس از صف خارج می‌شود و در تاریخچه ثبت می‌شود. |
| `GET` | `review/changes` | سجل اقدامات نصاب‌ها | قابلیت «بازبینی اقدامات نصاب‌ها» (پیش‌فرض فقط مدیر کل) | همه ثبت/ویرایش/حذف‌های کاربران غیرمدیر (نصاب/اپراتور ثبت/…) بر حسب روز، با خلاصه تغییرات و وضعیت بازبینی (pending/kept/reverted). فیلتر: date یا from&to + user_id + status. |
| `POST` | `review/keep` | نگه‌داشتن تغییر | بازبینی اقدامات نصاب‌ها | {id} — علامت «بازبینی شد/نگه داشته شد»؛ تغییر باقی می‌ماند (پیش‌فرض همه تغییرات می‌مانند). |
| `POST` | `review/revert` | بازگردانی به حالت قبل | بازبینی اقدامات نصاب‌ها | {id} — وضعیت دقیق قبل از تغییر بازمی‌گردد: ثبت → سرویس حذف می‌شود؛ ویرایش → ستون‌های سرویس/آدرس عیناً بازنویسی؛ حذف → سرویس با همان شناسه بازساز. اگر سرویس بعد از تغییر دوباره ویرایش شده باشد بازگردانی انجام نمی‌شود (محافظ تغییرات جدیدتر). |

### GET `review/queue` — صف سرویس‌های ارجاعی

سرویس‌هایی که با دسته‌بندی پیش‌فرض «ثبت جهت بازبینی و ویرایش یا تأیید مدیریت» ثبت شده‌اند — همراه آدرس کامل/بلوک/پلاک/واحد، شماره مجازی، ثبت‌کننده، وضعیت دایری و فهرست دسته‌ها/تگ‌ها برای تعیین دسته. هر سرویسی که دسته مناسب ندارد می‌تواند با این دسته به بازبینی ارجاع شود.

دسترسی لازم: **قابلیت «بازبینی سرویس‌های ارجاعی» (پیش‌فرض فقط مدیر کل؛ از نقش‌ها قابل اعطا)**

### POST `review/assign` — تعیین دسته سرویس ارجاعی

{service_id, category, tags} — بازبین دسته‌بندی/تگ درست را تعیین می‌کند؛ سرویس از صف خارج می‌شود و در تاریخچه ثبت می‌شود.

دسترسی لازم: **بازبینی سرویس‌های ارجاعی**

نمونه بدنه:

```json
{
    "service_id": 12,
    "category": 5,
    "tags": [
        2,
        7
    ]
}
```

### GET `review/changes` — سجل اقدامات نصاب‌ها

همه ثبت/ویرایش/حذف‌های کاربران غیرمدیر (نصاب/اپراتور ثبت/…) بر حسب روز، با خلاصه تغییرات و وضعیت بازبینی (pending/kept/reverted). فیلتر: date یا from&to + user_id + status.

دسترسی لازم: **قابلیت «بازبینی اقدامات نصاب‌ها» (پیش‌فرض فقط مدیر کل)**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `date / from & to` | date | یک روز یا بازه YYYY-MM-DD |
| `user_id` | int | فیلتر کاربر (۰ = همه) |
| `status` | string | pending \| kept \| reverted \| خالی = همه |

### POST `review/keep` — نگه‌داشتن تغییر

{id} — علامت «بازبینی شد/نگه داشته شد»؛ تغییر باقی می‌ماند (پیش‌فرض همه تغییرات می‌مانند).

دسترسی لازم: **بازبینی اقدامات نصاب‌ها**

نمونه بدنه:

```json
{
    "id": 101
}
```

### POST `review/revert` — بازگردانی به حالت قبل

{id} — وضعیت دقیق قبل از تغییر بازمی‌گردد: ثبت → سرویس حذف می‌شود؛ ویرایش → ستون‌های سرویس/آدرس عیناً بازنویسی؛ حذف → سرویس با همان شناسه بازساز. اگر سرویس بعد از تغییر دوباره ویرایش شده باشد بازگردانی انجام نمی‌شود (محافظ تغییرات جدیدتر).

دسترسی لازم: **بازبینی اقدامات نصاب‌ها**

نمونه بدنه:

```json
{
    "id": 101
}
```

## ایمپورت اکسل (۳ مرحله‌ای)

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `POST` | `import/preview` | مرحله ۱ — آپلود و نگاشت | ایمپورت | فایل xlsx multipart → سرستون‌ها + پیش‌نمایش + پیشنهاد نگاشت ستون‌ها. |
| `POST` | `import/analyze` | مرحله ۲ — تحلیل تشابه | ایمپورت | ردیف‌های مشابه با داده‌های موجود (کلید تطبیق) + وضعیت هر ردیف. |
| `POST` | `import/commit` | مرحله ۳ — ثبت نهایی | ایمپورت | ثبت ردیف‌ها با تصمیم هر ردیف (ایجاد/ادغام/رد). |

### POST `import/preview` — مرحله ۱ — آپلود و نگاشت

فایل xlsx multipart → سرستون‌ها + پیش‌نمایش + پیشنهاد نگاشت ستون‌ها.

دسترسی لازم: **ایمپورت**

### POST `import/analyze` — مرحله ۲ — تحلیل تشابه

ردیف‌های مشابه با داده‌های موجود (کلید تطبیق) + وضعیت هر ردیف.

دسترسی لازم: **ایمپورت**

نمونه بدنه:

```json
{
    "session_id": "…",
    "match_key": "f_phone"
}
```

### POST `import/commit` — مرحله ۳ — ثبت نهایی

ثبت ردیف‌ها با تصمیم هر ردیف (ایجاد/ادغام/رد).

دسترسی لازم: **ایمپورت**

نمونه بدنه:

```json
{
    "session_id": "…",
    "rows": []
}
```

## خروجی‌ها و پشتیبان

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `export/xlsx` | خروجی اکسل | مشاهده یا خروجی | نتیجه جستجو/فیلتر فعلی (همان پارامترهای search — شامل progress_status/progress_step). ۱.۱۲.۰: ستون‌های «وضعیت دایری»، «آخرین مرحله دایری» و «خرابی اعلام‌شده» به خروجی اضافه شد. |
| `GET` | `export/pdf` | خروجی PDF بومی | مشاهده یا خروجی | PDF با فونت فارسی تعبیه‌شده + ستون‌های پیشرفت دایری (۱.۱۲.۰). |
| `GET` | `export/print` | صفحه چاپ | مشاهده یا خروجی | HTML آماده چاپ مرورگر + ستون‌های پیشرفت دایری (۱.۱۲.۰). |
| `GET` | `template` | فایل نمونه اکسل | کاربر افزونه | قالب خالی با ستون‌های فعلی. |
| `GET` | `backup` | پشتیبان JSON | تنظیمات | ?with_history=1&with_activity=1&with_sms_log=1 — قابل بازیابی روی هر سرور/دامنه. |
| `GET` | `backup/info` | اطلاعات پشتیبان | تنظیمات | تعداد رکورد هر جدول + حجم تقریبی + محدودیت‌های PHP. |
| `GET` | `backup/sql` | پشتیبان SQL قابل‌حمل | تنظیمات | دستورات SQL با {{PFX}} — مستقل از پیشوند جدول. |
| `GET` | `backup/zip` | پشتیبان ZIP کامل | تنظیمات | دیتابیس + تاریخچه + فایل‌های افزونه + راهنمای بازیابی. |
| `POST` | `restore` | بازیابی مستقیم | تنظیمات | فایل JSON (بدنه json یا فایل file) — گزارش شمارش/خطاها. |
| `POST` | `restore/begin` | آپلود تکه‌ای — شروع | تنظیمات | فایل بزرگ بدون محدودیت upload_max_filesize → {token, chunk_size}. |
| `POST` | `restore/chunk` | آپلود تکه‌ای — تکه | تنظیمات | {token, index, data} به‌ترتیب. |
| `POST` | `restore/finish` | آپلود تکه‌ای — پایان | تنظیمات | سرهم‌کردن + اجرای بازیابی + خلاصه نتیجه. |
| `POST` | `restore/cancel` | لغو نشست بازیابی | تنظیمات | پاک‌سازی فایل‌های موقت. |
| `GET` | `backup/list` | فهرست پشتیبان‌های سرور | تنظیمات | همه پشتیبان‌های ذخیره‌شده (خودکارِ قبل از ایمپورت + دستی) + وضعیت محل ذخیره (پوشه افزونه/uploads). |
| `POST` | `backup/stored` | پشتیبان روی سرور — ساخت | تنظیمات | گرفتن پشتیبان کامل تازه و ذخیره روی سرور؛ خروجی متادیتای فایل. |
| `POST` | `backup/stored/restore` | بازگردانی یک‌کلیکی | تنظیمات | بازیابی کامل از فایل پشتیبان ذخیره‌شده روی سرور — بدون آپلود فایل. |
| `POST` | `backup/stored/delete` | حذف پشتیبان سرور | تنظیمات | حذف فایل پشتیبان و متادیتای آن. |
| `GET` | `backup/stored/download` | دانلود پشتیبان سرور | تنظیمات | ?filename= — همان فایل JSON پشتیبان برای نگهداری بیرونی. |
| `POST` | `numbers/fix` | اصلاح اعداد فارسی/عربی → انگلیسی | تنظیمات | تبدیل ارقام ۰-۹ و ٠-٩ به 0-9 در همه فیلدهای سرویس/آدرس + متن گزارش‌های کار؛ پیش از اجرا پشتیبان کامل خودکار گرفته می‌شود و در پاسخ نام فایل آن می‌آید. ?dry_run=true فقط می‌شمارد. |

### GET `export/xlsx` — خروجی اکسل

نتیجه جستجو/فیلتر فعلی (همان پارامترهای search — شامل progress_status/progress_step). ۱.۱۲.۰: ستون‌های «وضعیت دایری»، «آخرین مرحله دایری» و «خرابی اعلام‌شده» به خروجی اضافه شد.

دسترسی لازم: **مشاهده یا خروجی**

### GET `export/pdf` — خروجی PDF بومی

PDF با فونت فارسی تعبیه‌شده + ستون‌های پیشرفت دایری (۱.۱۲.۰).

دسترسی لازم: **مشاهده یا خروجی**

### GET `export/print` — صفحه چاپ

HTML آماده چاپ مرورگر + ستون‌های پیشرفت دایری (۱.۱۲.۰).

دسترسی لازم: **مشاهده یا خروجی**

### GET `template` — فایل نمونه اکسل

قالب خالی با ستون‌های فعلی.

دسترسی لازم: **کاربر افزونه**

### GET `backup` — پشتیبان JSON

?with_history=1&with_activity=1&with_sms_log=1 — قابل بازیابی روی هر سرور/دامنه.

دسترسی لازم: **تنظیمات**

### GET `backup/info` — اطلاعات پشتیبان

تعداد رکورد هر جدول + حجم تقریبی + محدودیت‌های PHP.

دسترسی لازم: **تنظیمات**

### GET `backup/sql` — پشتیبان SQL قابل‌حمل

دستورات SQL با {{PFX}} — مستقل از پیشوند جدول.

دسترسی لازم: **تنظیمات**

### GET `backup/zip` — پشتیبان ZIP کامل

دیتابیس + تاریخچه + فایل‌های افزونه + راهنمای بازیابی.

دسترسی لازم: **تنظیمات**

### POST `restore` — بازیابی مستقیم

فایل JSON (بدنه json یا فایل file) — گزارش شمارش/خطاها.

دسترسی لازم: **تنظیمات**

### POST `restore/begin` — آپلود تکه‌ای — شروع

فایل بزرگ بدون محدودیت upload_max_filesize → {token, chunk_size}.

دسترسی لازم: **تنظیمات**

نمونه بدنه:

```json
{
    "size": 5242880,
    "name": "backup.json"
}
```

### POST `restore/chunk` — آپلود تکه‌ای — تکه

{token, index, data} به‌ترتیب.

دسترسی لازم: **تنظیمات**

نمونه بدنه:

```json
{
    "token": "…",
    "index": 0,
    "data": "…"
}
```

### POST `restore/finish` — آپلود تکه‌ای — پایان

سرهم‌کردن + اجرای بازیابی + خلاصه نتیجه.

دسترسی لازم: **تنظیمات**

نمونه بدنه:

```json
{
    "token": "…"
}
```

### POST `restore/cancel` — لغو نشست بازیابی

پاک‌سازی فایل‌های موقت.

دسترسی لازم: **تنظیمات**

نمونه بدنه:

```json
{
    "token": "…"
}
```

### GET `backup/list` — فهرست پشتیبان‌های سرور

همه پشتیبان‌های ذخیره‌شده (خودکارِ قبل از ایمپورت + دستی) + وضعیت محل ذخیره (پوشه افزونه/uploads).

دسترسی لازم: **تنظیمات**

### POST `backup/stored` — پشتیبان روی سرور — ساخت

گرفتن پشتیبان کامل تازه و ذخیره روی سرور؛ خروجی متادیتای فایل.

دسترسی لازم: **تنظیمات**

نمونه بدنه:

```json
{
    "context": "manual"
}
```

### POST `backup/stored/restore` — بازگردانی یک‌کلیکی

بازیابی کامل از فایل پشتیبان ذخیره‌شده روی سرور — بدون آپلود فایل.

دسترسی لازم: **تنظیمات**

نمونه بدنه:

```json
{
    "filename": "tpp-backup-20250921-101500-ab12cd34.json"
}
```

### POST `backup/stored/delete` — حذف پشتیبان سرور

حذف فایل پشتیبان و متادیتای آن.

دسترسی لازم: **تنظیمات**

نمونه بدنه:

```json
{
    "filename": "tpp-backup-….json"
}
```

### GET `backup/stored/download` — دانلود پشتیبان سرور

?filename= — همان فایل JSON پشتیبان برای نگهداری بیرونی.

دسترسی لازم: **تنظیمات**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `filename` | string | نام فایل از backup/list |

### POST `numbers/fix` — اصلاح اعداد فارسی/عربی → انگلیسی

تبدیل ارقام ۰-۹ و ٠-٩ به 0-9 در همه فیلدهای سرویس/آدرس + متن گزارش‌های کار؛ پیش از اجرا پشتیبان کامل خودکار گرفته می‌شود و در پاسخ نام فایل آن می‌آید. ?dry_run=true فقط می‌شمارد.

دسترسی لازم: **تنظیمات**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `dry_run` | boolean | فقط شمارش بدون تغییر (پیش‌نمایش) |

نمونه بدنه:

```json
{
    "dry_run": false
}
```

## مدیریت ساختار و تنظیمات

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `fields` | فهرست فیلدها | کاربر افزونه | بدون دسترسی مدیریت فقط فیلدهای قابل مشاهده کاربر. |
| `POST` | `fields` | افزودن فیلد | مدیریت فیلدها | ستون واقعی جدید + بایگانی خودکار هنگام حذف. |
| `PUT` | `fields/{id}` | ویرایش فیلد | مدیریت فیلدها |  |
| `DELETE` | `fields/{id}` | حذف فیلد | مدیریت فیلدها | ?archive=1 مقادیر بایگانی شوند. |
| `POST` | `fields/reorder` | جابه‌جایی فیلدها | مدیریت فیلدها | {group, order:[ids]} — ترتیب فرم‌ها/جدول‌ها. |
| `GET` | `roles` | فهرست نقش‌ها | مدیریت نقش‌ها | نقش‌های tpp_ با قابلیت‌ها و فیلدهای مجاز. |
| `POST` | `roles` | افزودن نقش | مدیریت نقش‌ها |  |
| `PUT` | `roles/{slug}` | ویرایش نقش | مدیریت نقش‌ها |  |
| `DELETE` | `roles/{slug}` | حذف نقش | مدیریت نقش‌ها | نقش‌های پیش‌فرض فقط ویرایش می‌شوند. |
| `GET` | `users` | کاربران افزونه | مدیریت نقش‌ها | کاربران دارای دسترسی + نقش‌های tpp آن‌ها — برای اتصال افزونه‌های دیگر. |
| `GET` | `settings` | خواندن تنظیمات | تنظیمات | همه تنظیمات (جستجو/تاریخچه/پیامک/…). |
| `PUT` | `settings` | ذخیره تنظیمات | تنظیمات | کلیدهای مجاز sanitize می‌شوند. |
| `GET` | `stats` | آمار کلی | مشاهده سرویس‌ها | شمار سرویس/آدرس/کاربران فعال. |

### GET `fields` — فهرست فیلدها

بدون دسترسی مدیریت فقط فیلدهای قابل مشاهده کاربر.

دسترسی لازم: **کاربر افزونه**

### POST `fields` — افزودن فیلد

ستون واقعی جدید + بایگانی خودکار هنگام حذف.

دسترسی لازم: **مدیریت فیلدها**

نمونه بدنه:

```json
{
    "group": "service",
    "label": "نام فیلد",
    "field_type": "text",
    "is_searchable": 1
}
```

### PUT `fields/{id}` — ویرایش فیلد



دسترسی لازم: **مدیریت فیلدها**

### DELETE `fields/{id}` — حذف فیلد

?archive=1 مقادیر بایگانی شوند.

دسترسی لازم: **مدیریت فیلدها**

### POST `fields/reorder` — جابه‌جایی فیلدها

{group, order:[ids]} — ترتیب فرم‌ها/جدول‌ها.

دسترسی لازم: **مدیریت فیلدها**

نمونه بدنه:

```json
{
    "group": "service",
    "order": [
        3,
        1,
        2
    ]
}
```

### GET `roles` — فهرست نقش‌ها

نقش‌های tpp_ با قابلیت‌ها و فیلدهای مجاز.

دسترسی لازم: **مدیریت نقش‌ها**

### POST `roles` — افزودن نقش



دسترسی لازم: **مدیریت نقش‌ها**

نمونه بدنه:

```json
{
    "label": "نقش جدید",
    "caps": {
        "tpp_view_services": 1
    },
    "fields": []
}
```

### PUT `roles/{slug}` — ویرایش نقش



دسترسی لازم: **مدیریت نقش‌ها**

### DELETE `roles/{slug}` — حذف نقش

نقش‌های پیش‌فرض فقط ویرایش می‌شوند.

دسترسی لازم: **مدیریت نقش‌ها**

### GET `users` — کاربران افزونه

کاربران دارای دسترسی + نقش‌های tpp آن‌ها — برای اتصال افزونه‌های دیگر.

دسترسی لازم: **مدیریت نقش‌ها**

پارامترها:

| نام | نوع | توضیح |
|---|---|---|
| `q` | string | جستجو در نام |

### GET `settings` — خواندن تنظیمات

همه تنظیمات (جستجو/تاریخچه/پیامک/…).

دسترسی لازم: **تنظیمات**

### PUT `settings` — ذخیره تنظیمات

کلیدهای مجاز sanitize می‌شوند.

دسترسی لازم: **تنظیمات**

نمونه بدنه:

```json
{
    "rows_per_page": 25,
    "search_dedupe_seconds": 15
}
```

### GET `stats` — آمار کلی

شمار سرویس/آدرس/کاربران فعال.

دسترسی لازم: **مشاهده سرویس‌ها**

## پیامک (SMS.ir)

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `sms/status` | وضعیت پنل | ارسال پیامک | اتصال + موجودی باقیمانده (?fresh=1 بدون کش). |
| `POST` | `sms/send` | ارسال پیامک | ارسال پیامک | برای سرویس با قالب یا شماره/متن دلخواه. |
| `GET` | `sms/templates` | قالب‌ها | ارسال پیامک | فهرست قالب‌های پیامک. |
| `POST` | `sms/templates` | قالب جدید | تنظیمات |  |
| `PUT` | `sms/templates/{id}` | ویرایش قالب | تنظیمات |  |
| `DELETE` | `sms/templates/{id}` | حذف قالب | تنظیمات |  |
| `GET` | `sms/log` | گزارش ارسال‌ها | تنظیمات | آخرین ارسال‌ها با وضعیت. |

### GET `sms/status` — وضعیت پنل

اتصال + موجودی باقیمانده (?fresh=1 بدون کش).

دسترسی لازم: **ارسال پیامک**

### POST `sms/send` — ارسال پیامک

برای سرویس با قالب یا شماره/متن دلخواه.

دسترسی لازم: **ارسال پیامک**

نمونه بدنه:

```json
{
    "service_id": 12,
    "template_id": 2
}
```

### GET `sms/templates` — قالب‌ها

فهرست قالب‌های پیامک.

دسترسی لازم: **ارسال پیامک**

### POST `sms/templates` — قالب جدید



دسترسی لازم: **تنظیمات**

نمونه بدنه:

```json
{
    "title": "خوش‌آمد",
    "body": "{{f_owner_name}} عزیز…"
}
```

### PUT `sms/templates/{id}` — ویرایش قالب



دسترسی لازم: **تنظیمات**

### DELETE `sms/templates/{id}` — حذف قالب



دسترسی لازم: **تنظیمات**

### GET `sms/log` — گزارش ارسال‌ها

آخرین ارسال‌ها با وضعیت.

دسترسی لازم: **تنظیمات**

## مرکز API و توکن‌ها

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `GET` | `api/tokens` | توکن‌های من | کاربر افزونه | فهرست دستگاه‌ها/توکن‌های کاربر فعلی (بدون مقدار خام)؛ مدیر می‌تواند ?user_id= بدهد. |
| `POST` | `api/tokens` | صدور توکن API | کاربر افزونه | برای اتصال افزونه‌ها/اسکریپت‌های دیگر؛ مقدار خام فقط یک‌بار برمی‌گردد. |
| `DELETE` | `api/tokens` | ابطال توکن | کاربر افزونه | {idx} از فهرست توکن‌های من؛ مدیر {user_id} هم می‌دهد. |
| `GET` | `api/docs` | مستندات زنده | کاربر افزونه | همین کاتالوگ به‌صورت JSON برای رندر در ابزارهای دیگر؛ ?format=md نسخه Markdown دانلود می‌کند. |

### GET `api/tokens` — توکن‌های من

فهرست دستگاه‌ها/توکن‌های کاربر فعلی (بدون مقدار خام)؛ مدیر می‌تواند ?user_id= بدهد.

دسترسی لازم: **کاربر افزونه**

### POST `api/tokens` — صدور توکن API

برای اتصال افزونه‌ها/اسکریپت‌های دیگر؛ مقدار خام فقط یک‌بار برمی‌گردد.

دسترسی لازم: **کاربر افزونه**

نمونه بدنه:

```json
{
    "label": "اتصال افزونه فروش"
}
```

### DELETE `api/tokens` — ابطال توکن

{idx} از فهرست توکن‌های من؛ مدیر {user_id} هم می‌دهد.

دسترسی لازم: **کاربر افزونه**

نمونه بدنه:

```json
{
    "idx": 2
}
```

### GET `api/docs` — مستندات زنده

همین کاتالوگ به‌صورت JSON برای رندر در ابزارهای دیگر؛ ?format=md نسخه Markdown دانلود می‌کند.

دسترسی لازم: **کاربر افزونه**

## همگام‌سازی آفلاین

| متد | مسیر | عنوان | دسترسی | توضیح |
|---|---|---|---|---|
| `POST` | `sync` | دسته عملیات آفلاین | کاربر افزونه | صف عملیات ثبت/ویرایش/حذف با op_id یکتا (idempotent) + داده تازه برای کش — هسته کار آفلاین PWA. |

### POST `sync` — دسته عملیات آفلاین

صف عملیات ثبت/ویرایش/حذف با op_id یکتا (idempotent) + داده تازه برای کش — هسته کار آفلاین PWA.

دسترسی لازم: **کاربر افزونه**

نمونه بدنه:

```json
{
    "ops": [
        {
            "op_id": "uniq-3",
            "kind": "service.create",
            "payload": []
        }
    ]
}
```

## خطاها

همه خطاها با ساختار `{"code": "…", "message": "…", "data": {"status": 4xx}}` برمی‌گردند؛ کدهای رایج: `tpp_forbidden` (۴۰۳)، `tpp_unauthorized` (۴۰۱)، `tpp_not_found` (۴۰۴)، `tpp_rest_no_route` (۴۰۴)، `tpp_server_error` (۵۰۰).

## اتصال از PHP (داخل افزونه‌های دیگر)

علاوه بر REST، PHP API داخلی هم هست — `tpp()->services()->search( array( 'query' => 'کدپستی' ) )`. فهرست کامل در `DEVELOPERS.md` پوشه افزونه.

```php
// نمونه فراخوانی REST از PHP با توکن
$r = wp_remote_get( 'https://test.example.com/wp-json/tpp/v1/search?query=تست', array(
  'headers' => array( 'X-TPP-Token' => $tpp_token ),
) );
$data = json_decode( wp_remote_retrieve_body( $r ), true );
```
