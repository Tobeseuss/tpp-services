# راهنمای مشارکت و جریان کاری (CONTRIBUTING)

> ⚡️ **اصل اساسی این پروژه:** مانند یک دولوپر حرفه‌ای با گیت‌هاب کار می‌کنیم — **بعد از هر تغییر، یک کامیت مستقل و push به ریپو انجام می‌شود و فایل‌های zip پروژه به‌عنوان Release منتشر می‌گردند.** هیچ تغییری بدون کامیت/push رها نمی‌شود.

## ۱. جریان کار هر تغییر

1. تغییر را در شاخه `main` اعمال کن (پروژه تک‌توسعه‌گیرنده است؛ برچسب شاخه موقت لازم نیست)
2. پیش از کامیت، صحت کد را بررسی کن:
   - فایل‌های JS: `node --check <file>`
   - کلاس‌های PHP: بررسی ساختار (کلاس‌ها بدون خطای سینتکس)
   - `git status` — مطمئن شو فقط فایل‌های مرتبط در تغییر هستند (نویز مجوز فایل‌ها را با `chmod 644` نرمال کن و کامیت نکن)
3. **یک کامیت مستقل برای هر تغییر** با پیام متعارف (Conventional Commits):

```
feat(scope): توضیح کوتاه انگلیسی

- جزئیات فارسی/انگلیسی تغییرات
- ارجاع به درخواست کاربر در صورت وجود
```

انواع مجاز: `feat` / `fix` / `docs` / `refactor` / `chore` / `perf`

4. **بلافاصله push** کن: `git push origin main`

## ۲. نسخه‌بندی — هشت نقطه هم‌زمان

هر نسخه جدید (حتی patch) باید **همه نقاط زیر** هم‌زمان به‌روز شوند، وگرنه کش PWA نسخه قدیمی را سرو می‌کند:

| # | نقطه | فایل |
|---|------|------|
| ۱ | هدر `Version:` | `tpp-services.php` |
| ۲ | `$tpp_this_version` | `tpp-services.php` |
| ۳ | `TPP_VERSION` | `tpp-services.php` |
| ۴ | `TPP_DB_VERSION` (فقط با مهاجرت DB) | `tpp-services.php` |
| ۵ | `SW_VERSION` با الگوی `tpp-vX.Y.Z` | `pwa/sw.js` |
| ۶ | ۹ پارامتر `?v=X.Y.Z` | `pwa/index.html` |
| ۷ | `TPP.VERSION` | `pwa/assets/js/tpp-app.js` |
| ۸ | `Stable tag` + ورودی `== Changelog ==` | `readme.txt` |

> ⚠️ درس‌های گذشته: در ۱.۲۱.۰ نقطه ۷ جا ماند (اعلان بروزرسانی خراب شد) و callback تقویم به «تابع رندر» پاس شد ولی نه به «تابع اتصال‌دهنده رویداد» (کلیک‌ها بی‌صدا از کار افتاد) — این دو را همیشه چک کنید.

## ۳. انتشار Release

Release فقط همراه **تغییر نسخه** منتشر می‌شود (تغییر صرفاً مستندات/ابزار = فقط کامیت و push):

1. بسته نصب‌شونده را بسازید: zip با پوشه ریشه `tpp-services/` (دو نام: `tpp-services-vX.Y.Z.zip` و `tpp-services.zip`)
2. تگ `vX.Y.Z` روی همان کامیت `main` + یادداشت انتشار فارسی (رفع باگ‌ها / قابلیت‌ها / REST جدید / دستور نصب و ارتقا / MD5)
3. هر دو فایل zip به‌عنوان asset به Release پیوست شود

## ۴. امنیت

- توکن دسترسی فقط به‌صورت موقت در خط فرمان/حافظه استفاده می‌شود؛ **هرگز** در ریپو، فایل track‌شده یا remote URL دائمی ذخیره نشود
- remote همیشه بدون توکن باشد: `https://github.com/Tobeseuss/tpp-services.git`
- پس از اتمام دوره استفاده، توکن از تنظیمات گیت‌هاب (Settings → Developer settings) تمدید/لغو شود
